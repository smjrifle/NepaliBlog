<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.vimvox.nepaliblog.MainActivity$PlaceholderFragment" >

     <ListView
        android:id="@+id/list"
        android:layout_below="@+id/prev"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >
    </ListView>

    <TextView
        android:id="@+id/noConnection"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Sorry :(,\nthere seems to be no internet connection to view this page, however you can visit other pages to get information about donation and ktmDrive :).\nThank you"
        android:textSize="20sp"
        android:visibility="gone" />

    <Button
        android:id="@+id/retry"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/noConnection"
        android:text="Click to Retry"
        android:visibility="gone" >
    </Button>

</RelativeLayout>
